# --- Sampling ---
temperature: 1.0
sampling_top_p: null
sampling_top_k: null

# --- Model / hardware ---
max_model_len: null  # auto-detected from model config
tensor_parallel_size: 1
gpu_memory_utilization: 0.85
